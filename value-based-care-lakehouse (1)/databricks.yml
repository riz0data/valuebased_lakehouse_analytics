# Databricks Asset Bundle - PLACEHOLDER
# Will define the Databricks jobs/workflows that orchestrate:
#   1. Ingestion (DE-SynPUF, Synthea, NPPES landing scripts)
#   2. dbt build (Bronze -> Silver Data Vault -> Gold -> Semantic Layer)
#   3. CI validation on every pull request

bundle:
  name: value_based_care_lakehouse

targets:
  dev:
    default: true
    workspace:
      host: https://<your-databricks-workspace>.cloud.databricks.com

# resources:
#   jobs:
#     ingestion_job: ...
#     dbt_build_job: ...
